<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>make-infoへようこそ！</h1>
      <%= link_to '新規登録する', signup_path, class: 'btn btn-lg btn-success' %>
    </div>
  </div>
<% end %>
